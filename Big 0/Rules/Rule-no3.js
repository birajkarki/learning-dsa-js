function compressBoxesTwice(boxes1, boxes2) {
  boxes1.forEach(function (boxes) {
    console.log(boxes);
  });
  boxes2.forEach(function (boxes) {
    console.log(boxes);
  });
}

// BIG 0 ( a + b  )

// Different terms for inputs : Rule no 3

// Use different terms for different inputs. When analyzing the time complexity of an algorithm with multiple input
// variables, it is important to use different terms for each input variable in the time complexity expression.
